# .devcontainer/docker-compose.yml
services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
      args:
        DOCKER_GID: "${DOCKER_GID:-1000}"
    volumes:
      - ..:/workspace:cached
      - go-cache:/go
      - /var/run/docker.sock:/var/run/docker.sock
    command: sleep infinity
    environment:
      - CRAPP_SERVER_SESSION_SECRET=${SESSION_SECRET}
      - CRAPP_DATABASE_PASSWORD=${POSTGRES_PASSWORD}
      - CRAPP_DATABASE_USER=${POSTGRES_USER}
      - CRAPP_DATABASE_DBNAME=${POSTGRES_DB}
      - CRAPP_DATABASE_PORT=${POSTGRES_PORT}
    depends_on:
      - db
    networks:
      - crapp-network

  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_DB=${POSTGRES_DB}
      - PGPORT=${POSTGRES_PORT}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT}:${POSTGRES_PORT}"
      #- "5431:5431"
    networks:
      - crapp-network

networks:
  crapp-network:
    driver: bridge

volumes:
  postgres-data:
  go-cache: