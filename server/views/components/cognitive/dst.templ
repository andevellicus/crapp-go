package cognitive

templ DST(questionID string, settingsJSON string, cspNonce string) {
	<div 
		id="dst-container" 
		class="p-4 text-center" 
		data-question-id={ questionID } 
		data-settings={ settingsJSON }
	>
		<p>Initializing Test...</p>
	</div>
}